---
import BLayout from "@/Layout.astro";
import { CardsGr, DataCard } from "@/Widgets/Cards";
import { EmoBtn, EmoCall, Emograph, EmoList, HzStack, IndexBtn, SecTitle, VrStack } from "@/Widgets/Comp";
import { ESec, Hero, IndexSec, ReadAlso, ReadAlsoItem } from "@/Widgets/Sections";
import { GlassCard } from "@/Widgets/Shapes";
import { EmoContactStrip, EmoStrip } from "@/Widgets/Strips";
import { Align, Colors, ElemBg, EmoMediaData, Loc, NumPer, RespString, Size, Text, Topic } from "@/TypesLib";
---




















<BLayout
	pSchemaType="CollectionPage"
	pTitle="خدمات التشطيبات"
	pDescr="خدمات تشطيب الشقق والفلل والمحلات والصيدليات من خلال إدارة هندسية منظمة تبدأ من المعاينة والمقايسة واختيار مستوى التشطيب وتنتهي بالتسليم النهائي."
>







	<Hero>

		<VrStack
			pLoc={new Loc(new NumPer("0%","2.5%"),new NumPer("35%","20%"))}
			pSize={new Size(new NumPer("40%","95%"))}
			pHzAlign={"Center"}
		>

			<Emograph pTitle={Text.H1.Centered.White.OfText("خدمات تشطيبات متكاملة")} />

			<EmoCall BlackSky />

		</VrStack>

	</Hero>







</BLayout>
