---
import { Place, type PlaceProps } from "@/TypesLib";
import type { HTMLAttributes } from "astro/types";

interface Props extends DebugOutlineProps, HTMLAttributes<"section">, PlaceProps {
	/** أتعاب الخدمة المباشرة لشهادة صلاحية المبنى للإشغال للمبنى السكني. */
	pServiceFee: number;

	/** أتعاب الخدمة للمبنى التجاري أو الإداري أو الصناعي. */
	pNonResidentialServiceFee: number;
}

const cProps = Astro.props as Props & Record<string, unknown>;

const {
	pServiceFee,
	pNonResidentialServiceFee,

	pLoc,
	pSize,
	pRotate,
	pZ,
	pOrigin,
	pOpacity,
	pClickable,
	pLandOnly,
	pPortOnly,
	pMove,
	pMoveDuration,
	pMoveLoc,
	pMoveBox,
	pMoveDistance,
	pMoveKeyPoints,
	pMoveIntervals,
	pMoveDelay,
	pMoveEase,
	pMoveLoop,
	pMoveLoopFor,
	pMoveLoopInterval,
	pMoveReverse,
	pMoveStartOnView,
	pMoveReplayOnView,
	pMovePauseWhenOut,
	pMoveViewThreshold,
	pMoveViewMargin,
	pMoveViewTarget,

	id: pGivenID = GetWidgetId("OccupancyValidityCostCalc"),
	class: pGivenClass = "",
	style: pGivenStyle = "",
	...RemAttr
} = cProps;

const cStyle = [
	"--CertificateAccent:#8b1e2d;",
	"--CertificateAccentSoft:#f8ecee;",
	"--CertificateAccentDark:#711624;",
	GetStyleText(pGivenStyle),
].filter(Boolean).join(" ");

const cPlace = new Place(cProps as PlaceProps, cStyle);
const cBuildingFee = 900;
const cStampRate = 0.0045;

function GetStyleText(pStyle: unknown) {
	if (!pStyle) return "";
	if (typeof pStyle === "string") return pStyle;
	if (typeof pStyle !== "object") return "";

	return Object.entries(pStyle as Record<string, string | number | null | undefined>)
		.map(([pName, pValue]) => {
			if (pValue === undefined || pValue === null) return "";
			return `${GetCssName(pName)}:${pValue};`;
		})
		.filter(Boolean)
		.join(" ");
}

function GetCssName(pName: string) {
	if (pName.startsWith("--")) return pName;
	return pName.replace(/[A-Z]/g, (pChar) => `-${pChar.toLowerCase()}`);
}
---





<section
	class = {`CertificateCostCalc OccupancyValidityCostCalc ${cPlace.Class} ${pGivenClass}`.trim()}
	style = {cPlace.Style}
	id={pGivenID}
	dir="rtl"
	data-occupancy-validity-cost="true"
	data-service-fee={pServiceFee}
	data-non-residential-service-fee={pNonResidentialServiceFee}
	data-building-fee={cBuildingFee}
	data-stamp-rate={cStampRate}
	{...RemAttr}
>







	<header class = " CertificateHead ">

		<div class = " CertificateHeadText ">

			<p class = " CertificateEyebrow ">
				حاسبة تكلفة تقريبية
			</p>

			<h2>
				تكلفة شهادة صلاحية المبنى للإشغال
			</h2>

			<p>
				احسب أتعاب الخدمة ورسوم النقابة والدمغة الهندسية المتبقية حسب بيانات رخصة البناء.
			</p>

		</div>

		<div class = " CertificateRateBadge " aria-label="نسبة الدمغة الهندسية 0.45 بالمائة">

			<strong>
				0.45%
			</strong>

			<span>
				دمغة هندسية من تكلفة الرخصة
			</span>

		</div>

	</header>







	<div class = " CertificateBody ">

		<form class = " CertificateInputs " data-area="inputs">

			<fieldset class = " CertificateLocation ">

				<legend>

					نوع المبنى

				</legend>

				<div class = " CertificateLocationButtons ">

					<label class = " CertificateLocationButton ">

						<input
							data-field="buildingType"
							type="radio"
							name={`${pGivenID}-building-type`}
							value="residential"
							checked
						/>

						<span>
							سكني
						</span>

					</label>

					<label class = " CertificateLocationButton ">

						<input
							data-field="buildingType"
							type="radio"
							name={`${pGivenID}-building-type`}
							value="nonResidential"
						/>

						<span>
							تجاري أو إداري أو صناعي
						</span>

					</label>

				</div>

			</fieldset>

			<div class = " CertificateFixedFee ">

				<span>
					أتعاب الخدمة
				</span>

				<strong>

					<span data-out="serviceFeeInput">
						{pServiceFee.toLocaleString("ar-EG")}
					</span>

					جنيه

				</strong>

			</div>

			<fieldset class = " CertificateBuildingExtras ">

				<legend>

					المنشآت الإضافية المذكورة في رخصة البناء

				</legend>

				<p class = " CertificateBuildingBase ">
					أصل المبنى محسوب تلقائيًا كمبنى واحد.
				</p>

				<div class = " CertificateBuildingExtraOptions ">

					<label class = " CertificateBuildingExtraOption ">

						<input data-field="buildingExtra" type="checkbox" value="wall" />

						<span>
							سور
						</span>

					</label>

					<label class = " CertificateBuildingExtraOption ">

						<input data-field="buildingExtra" type="checkbox" value="electricRoom" />

						<span>
							غرفة كهرباء
						</span>

					</label>

					<label class = " CertificateBuildingExtraOption ">

						<input data-field="buildingExtra" type="checkbox" value="securityRoom" />

						<span>
							غرفة أمن
						</span>

					</label>

					<label class = " CertificateBuildingExtraOption ">

						<input data-field="buildingExtra" type="checkbox" value="elevator" />

						<span>
							يوجد مصعد
						</span>

					</label>

				</div>

				<small>

					كل بند محدد يضاف كمبنى مستقل عند حساب الرسوم الأساسية للنقابة بقيمة

					{cBuildingFee.toLocaleString("ar-EG")} جنيه.

				</small>

			</fieldset>

			<label class = " CertificateField ">

				<span>
					تكلفة الأعمال المسجلة في الرخصة
				</span>

				<div class = " CertificateInputUnit ">

					<input
						data-field="licenseCost"
						type="number"
						min="0"
						step="1000"
						value="0"
						inputmode="decimal"
					/>

					<b>

						جنيه

					</b>

				</div>

				<small>

					تستخدم هذه القيمة في حساب الدمغة فقط، وليست القيمة السوقية للعقار.

				</small>

			</label>

			<div class = " CertificateField ">

				<span>
					دمغة هندسية سبق سدادها
				</span>

				<div class = " CertificatePaidStampRow ">

					<label class = " CertificatePaidStampFull ">

						<input data-field="paidStampFull" type="checkbox" />

						<span>
							تم سدادها بالكامل
						</span>

					</label>

					<div class = " CertificateInputUnit ">

						<input
							data-field="paidStamp"
							type="number"
							min="0"
							step="100"
							value="0"
							inputmode="decimal"
						/>

						<b>

							جنيه

						</b>

					</div>

				</div>

				<small>

					اكتب صفر إذا لم يسبق سداد دمغة مثبتة بالرخصة أو بإيصال مستقل.

				</small>

			</div>

		</form>

		<div class = " CertificateResult " aria-live="polite">

			<p class = " CertificateResultLabel ">
				الإجمالي التقريبي المطلوب
			</p>

			<p class = " CertificateTotal ">

				<strong data-out="total">
					0
				</strong>

				<span>
					جنيه
				</span>

			</p>

			<div class = " CertificateBreakdown ">

				<div class = " CertificateRow ">

					<span>
						أتعاب الخدمة
					</span>

					<strong data-out="serviceFee">
						0
					</strong>

				</div>

				<div class = " CertificateRow ">

					<span>
						الرسوم الأساسية للنقابة
					</span>

					<strong data-out="basicFee">
						0
					</strong>

				</div>

				<div class = " CertificateRow ">

					<span>
						الدمغة الهندسية المحسوبة
					</span>

					<strong data-out="grossStamp">
						0
					</strong>

				</div>

				<div class = " CertificateRow Muted ">

					<span>
						المسدد سابقًا من الدمغة
					</span>

					<strong data-out="paidStamp">
						0
					</strong>

				</div>

				<div class = " CertificateRow StampDue ">

					<span>
						الدمغة المتبقية
					</span>

					<strong data-out="stampDue">
						0
					</strong>

				</div>

			</div>

			<p class = " CertificateStatus " data-out="status">
				الحسبة تشمل كامل الدمغة المستحقة وفق البيانات المدخلة.
			</p>

		</div>

	</div>







	<div class = " CertificateFormula ">

		<span>
			الإجمالي
		</span>

		<b>

			=

		</b>

		<span>
			أتعاب الخدمة
		</span>

		<b>

			+

		</b>

		<span>

			{cBuildingFee} × عدد المباني

		</span>

		<b>

			+

		</b>

		<span>
			الدمغة المتبقية
		</span>

	</div>







	<p class = " CertificateDisclaimer ">
		الناتج تقديري طبقًا لطريقة الحساب الموضحة بالصفحة. القيمة النهائية تعتمد على المستندات المثبتة بالرخصة وما سبق سداده فعليًا.
	</p>







	<script is:inline NoMove>



		(() =>
		{

			// ==================== Definitions: Variables & Methods ====================





			function fInitOccupancyValidityCostCalc()
			{
				const fRoot = document.currentScript?.closest("[data-occupancy-validity-cost]");
				if (!fRoot) return;
				const fForm = fRoot.querySelector('[data-area="inputs"]');
				const fBuildingTypes = [...fRoot.querySelectorAll('[data-field="buildingType"]')];
				const fBuildingExtras = [...fRoot.querySelectorAll('[data-field="buildingExtra"]')];
				const fLicenseCost = fRoot.querySelector('[data-field="licenseCost"]');
				const fPaidStamp = fRoot.querySelector('[data-field="paidStamp"]');
				const fPaidStampFull = fRoot.querySelector('[data-field="paidStampFull"]');
				const fOut = (pName) => fRoot.querySelector(`[data-out="${pName}"]`);
				const fResidentialServiceFee = fNumber(fRoot.dataset.serviceFee);
				const fNonResidentialServiceFee = fNumber(fRoot.dataset.nonResidentialServiceFee);
				const fBuildingFee = fNumber(fRoot.dataset.buildingFee, 900);
				const fStampRate = fNumber(fRoot.dataset.stampRate, 0.0045);
				function fArabicToEnglishDigits(pValue)
				{
					return String(pValue ?? "")
					.replace(/[٠-٩]/g, (pDigit) => String("٠١٢٣٤٥٦٧٨٩".indexOf(pDigit)))
					.replace(/[۰-۹]/g, (pDigit) => String("۰۱۲۳۴۵۶۷۸۹".indexOf(pDigit)))
					.replace(/,/g, ".");
				}
				function fNumber(pValue, pFallback = 0)
				{
					const fParsed = Number(fArabicToEnglishDigits(pValue));
					return Number.isFinite(fParsed) ? fParsed : pFallback;
				}
				function fMoney(pValue)
				{
					return Math.round(Math.max(0, pValue)).toLocaleString("ar-EG");
				}
				function fSet(pName, pValue)
				{
					const fElement = fOut(pName);
					if (fElement) fElement.textContent = pValue;
				}
				function fCalc()
				{
					const fBuildingType = fRoot.querySelector('[data-field="buildingType"]:checked')?.value || "residential";
					const fServiceFee = fBuildingType === "nonResidential"
					? fNonResidentialServiceFee
					: fResidentialServiceFee;
					const fBuildingCount = 1 + fBuildingExtras.filter((pField) => pField.checked).length;
					const fLicense = Math.max(0, fNumber(fLicenseCost?.value));
					const fBasicFee = fBuildingCount * fBuildingFee;
					const fGrossStamp = fLicense * fStampRate;
					if (fPaidStamp)
					{
						fPaidStamp.disabled = Boolean(fPaidStampFull?.checked);
						if (fPaidStampFull?.checked)
						{
							fPaidStamp.value = String(Math.round(fGrossStamp));
						}
					}
					const fPaid = Math.max(0, fNumber(fPaidStamp?.value));
					const fStampDue = Math.max(0, fGrossStamp - fPaid);
					const fTotal = fServiceFee + fBasicFee + fStampDue;
					fSet("serviceFeeInput", fMoney(fServiceFee));
					fSet("serviceFee", fMoney(fServiceFee));
					fSet("basicFee", fMoney(fBasicFee));
					fSet("grossStamp", fMoney(fGrossStamp));
					fSet("paidStamp", fMoney(Math.min(fPaid, fGrossStamp)));
					fSet("stampDue", fMoney(fStampDue));
					fSet("total", fMoney(fTotal));
					if (fGrossStamp > 0 && fPaid >= fGrossStamp)
					{
						fSet("status", "لا توجد دمغة متبقية وفق المبلغ المسدد الذي أدخلته.");
					} else if (fPaid > 0) {
						fSet("status", `تم خصم ${fMoney(Math.min(fPaid, fGrossStamp))} جنيه من الدمغة المحسوبة.`);
					} else {
						fSet("status", "الحسبة تشمل كامل الدمغة المستحقة وفق البيانات المدخلة.");
					}
				}
				if (fForm) fForm.addEventListener("submit", (pEvent) => pEvent.preventDefault());
				[
					fLicenseCost,
					fPaidStamp
				].forEach((pField) =>
					{
						pField?.addEventListener("input", fCalc);
						pField?.addEventListener("change", fCalc);
						pField?.addEventListener("focus", () => pField.select());
				});
				fBuildingTypes.forEach((pField) => pField.addEventListener("change", fCalc));
				fBuildingExtras.forEach((pField) => pField.addEventListener("change", fCalc));
				fPaidStampFull?.addEventListener("change", fCalc);
				fCalc();
			}





			// ==================== Execution Logic ====================

			fInitOccupancyValidityCostCalc();
		})();
	</script>







</section>





<style>



	.CertificateCostCalc {
		box-sizing: border-box;
		margin-inline: auto;
		border: 1px solid #eadfe1;
		background: linear-gradient(145deg, #ffffff, #fdf9fa 72%);
		color: #20272c;
		box-shadow: 0 18px 52px rgba(83, 35, 44, .10);
		direction: rtl;
	}

	.CertificateCostCalc * {
		box-sizing: border-box;
	}


	.CertificateHead {
		display: grid;
		align-items: center;
		margin-bottom: 24px;
	}


	.CertificateHeadText h2 {
		margin: 0;
		font-size: clamp(25px, 3vw, 40px);
		line-height: 1.35;
		color: #1b2328;
	}

	.CertificateHeadText p {
		margin: 0;
	}

	.CertificateHeadText > p:last-child {
		max-width: 72ch;
		margin-top: 9px;
		color: #6f696b;
		font-size: 15px;
		line-height: 1.9;
	}


	.CertificateEyebrow {
		width: fit-content;
		margin-bottom: 10px;
		padding: 6px 11px;
		border-radius: 999px;
		background: var(--CertificateAccentSoft);
		color: var(--CertificateAccentDark);
		font-size: 13px;
		font-weight: 900;
	}


	.CertificateRateBadge {
		padding: 16px;
		border-radius: 24px;
		background: var(--CertificateAccentSoft);
		color: var(--CertificateAccentDark);
		display: grid;
		place-items: center;
		align-content: center;
		gap: 8px;
	}

	.CertificateRateBadge strong {
		line-height: 1;
		direction: ltr;
	}

	.CertificateRateBadge span {
		font-size: 12px;
		font-weight: 800;
		line-height: 1.5;
	}


	.CertificateBody {
		display: grid;
		gap: 18px;
	}


	.CertificateInputs {
		display: grid;
		gap: 15px;
		align-content: start;
		border: 1px solid #ece4e6;
		background: #fff;
	}


	.CertificateLocation {
		min-width: 0;
		margin: 0;
		padding: 0;
		border: 0;
	}

	.CertificateLocation legend {
		display: block;
		margin-bottom: 8px;
		color: #463e40;
		font-size: 13px;
		font-weight: 900;
	}


	.CertificateLocationButtons {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 8px;
		padding: 5px;
		border-radius: 17px;
		background: #f3edef;
	}


	.CertificateLocationButton {
		position: relative;
		min-width: 0;
	}

	.CertificateLocationButton input {
		position: absolute;
		width: 1px;
		height: 1px;
		opacity: 0;
		pointer-events: none;
	}

	.CertificateLocationButton span {
		display: grid;
		place-items: center;
		min-height: 43px;
		padding: 8px 12px;
		border: 1px solid transparent;
		border-radius: 13px;
		color: #6e6265;
		font-size: 14px;
		font-weight: 900;
		text-align: center;
		cursor: pointer;
		transition: background-color .18s ease, color .18s ease, box-shadow .18s ease;
	}

	.CertificateLocationButton input:checked + span {
		background: #fff;
		color: var(--CertificateAccentDark);
		box-shadow: 0 5px 15px rgba(83, 35, 44, .11);
	}

	.CertificateLocationButton input:focus-visible + span {
		outline: 3px solid color-mix(in srgb, var(--CertificateAccent) 28%, transparent);
		outline-offset: 2px;
	}


	.CertificateFixedFee {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 12px;
		min-height: 54px;
		padding: 11px 14px;
		border-radius: 16px;
		background: var(--CertificateAccentSoft);
		color: var(--CertificateAccentDark);
	}

	.CertificateFixedFee span {
		font-size: 13px;
		font-weight: 900;
	}

	.CertificateFixedFee strong {
		font-size: 18px;
		white-space: nowrap;
	}


	.CertificateBuildingExtras {
		min-width: 0;
		margin: 0;
		padding: 0;
		border: 0;
	}

	.CertificateBuildingExtras legend {
		display: block;
		margin-bottom: 8px;
		color: #463e40;
		font-size: 13px;
		font-weight: 900;
	}

	.CertificateBuildingExtras small {
		display: block;
		margin-top: 6px;
		color: #92878a;
		font-size: 11px;
		font-weight: 700;
		line-height: 1.65;
	}


	.CertificateBuildingBase {
		margin: 0 0 8px;
		color: #6f696b;
		font-size: 12px;
		font-weight: 800;
		line-height: 1.65;
	}


	.CertificateBuildingExtraOptions {
		display: grid;
		grid-template-columns: repeat(4, minmax(0, 1fr));
		gap: 8px;
	}


	.CertificateBuildingExtraOption {
		position: relative;
		min-width: 0;
	}

	.CertificateBuildingExtraOption input {
		position: absolute;
		width: 1px;
		height: 1px;
		opacity: 0;
		pointer-events: none;
	}

	.CertificateBuildingExtraOption span {
		display: grid;
		place-items: center;
		min-height: 43px;
		padding: 8px 10px;
		border: 1px solid #e1d9db;
		border-radius: 13px;
		background: #fdfbfc;
		color: #6e6265;
		font-size: 13px;
		font-weight: 900;
		text-align: center;
		cursor: pointer;
		transition: background-color .18s ease, color .18s ease, border-color .18s ease, box-shadow .18s ease;
	}

	.CertificateBuildingExtraOption input:checked + span {
		border-color: var(--CertificateAccent);
		background: var(--CertificateAccentSoft);
		color: var(--CertificateAccentDark);
		box-shadow: 0 5px 15px rgba(83, 35, 44, .09);
	}

	.CertificateBuildingExtraOption input:focus-visible + span {
		outline: 3px solid color-mix(in srgb, var(--CertificateAccent) 28%, transparent);
		outline-offset: 2px;
	}


	.CertificateField {
		display: block;
	}

	.CertificateField > span {
		display: block;
		margin-bottom: 8px;
		color: #463e40;
		font-size: 13px;
		font-weight: 900;
	}

	.CertificateField > input {
		width: 100%;
		min-height: 48px;
		padding: 10px 13px;
		border: 1px solid #e1d9db;
		border-radius: 15px;
		background: #fdfbfc;
		color: #20272c;
		font-family: inherit;
		font-size: 15px;
		font-weight: 800;
		outline: none;
	}

	.CertificateField > input:focus {
		border-color: var(--CertificateAccent);
		box-shadow: 0 0 0 4px color-mix(in srgb, var(--CertificateAccent) 11%, transparent);
	}

	.CertificateField small {
		display: block;
		margin-top: 6px;
		color: #92878a;
		font-size: 11px;
		font-weight: 700;
		line-height: 1.65;
	}


	.CertificatePaidStampRow {
		display: grid;
		grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
		gap: 8px;
		align-items: stretch;
	}

	.CertificatePaidStampRow .CertificateInputUnit input:disabled {
		cursor: not-allowed;
		opacity: .62;
	}


	.CertificatePaidStampFull {
		display: flex;
		align-items: center;
		gap: 9px;
		min-width: 0;
		min-height: 48px;
		padding: 10px 13px;
		border: 1px solid #e1d9db;
		border-radius: 15px;
		background: #fdfbfc;
		color: #463e40;
		font-size: 13px;
		font-weight: 900;
		cursor: pointer;
	}

	.CertificatePaidStampFull input {
		width: 18px;
		height: 18px;
		margin: 0;
		accent-color: var(--CertificateAccent);
		flex: 0 0 auto;
	}

	.CertificatePaidStampFull:has(input:checked) {
		border-color: var(--CertificateAccent);
		background: var(--CertificateAccentSoft);
		color: var(--CertificateAccentDark);
	}

	.CertificatePaidStampFull:has(input:focus-visible) {
		outline: 3px solid color-mix(in srgb, var(--CertificateAccent) 28%, transparent);
		outline-offset: 2px;
	}


	.CertificateInputUnit {
		display: grid;
		grid-template-columns: minmax(0, 1fr) auto;
		align-items: center;
		width: 100%;
		min-height: 48px;
		overflow: hidden;
		border: 1px solid #e1d9db;
		border-radius: 15px;
		background: #fdfbfc;
		color: #20272c;
		font-family: inherit;
		font-size: 15px;
		font-weight: 800;
	}

	.CertificateInputUnit:focus-within {
		border-color: var(--CertificateAccent);
		box-shadow: 0 0 0 4px color-mix(in srgb, var(--CertificateAccent) 11%, transparent);
	}

	.CertificateInputUnit input {
		width: 100%;
		min-height: 46px;
		padding: 10px 13px;
		border: 0;
		background: transparent;
		color: inherit;
		font: inherit;
		outline: none;
	}

	.CertificateInputUnit b {
		padding-inline: 12px;
		border-right: 1px solid #e8e0e2;
		color: #8c7e82;
		font-size: 12px;
		white-space: nowrap;
	}


	.CertificateResult {
		display: grid;
		gap: 14px;
		align-content: start;
		border: 1px solid #ece4e6;
		background: linear-gradient(160deg, #24191b, #3a2227);
		color: #fff;
	}


	.CertificateResultLabel {
		margin: 0;
		color: rgba(255, 255, 255, .68);
		font-size: 13px;
		font-weight: 900;
	}


	.CertificateTotal {
		margin: 0;
		display: flex;
		align-items: baseline;
		gap: 8px;
		color: #fff;
	}

	.CertificateTotal strong {
		font-size: clamp(40px, 6vw, 66px);
		line-height: 1;
		color: #ffb9c3;
	}

	.CertificateTotal span {
		font-size: 16px;
		font-weight: 900;
		color: rgba(255, 255, 255, .76);
	}


	.CertificateBreakdown {
		display: grid;
		gap: 7px;
	}


	.CertificateRow {
		display: grid;
		grid-template-columns: minmax(0, 1fr) auto;
		gap: 12px;
		align-items: center;
		min-height: 45px;
		padding: 9px 11px;
		border-radius: 13px;
		background: rgba(255, 255, 255, .075);
	}

	.CertificateRow span {
		font-size: 12px;
		font-weight: 800;
		color: rgba(255, 255, 255, .73);
	}

	.CertificateRow strong {
		font-size: 15px;
		color: #fff;
	}


	.CertificateStatus {
		margin: 0;
		padding-top: 2px;
		color: #d2c0c4;
		font-size: 11px;
		font-weight: 700;
		line-height: 1.7;
	}


	.CertificateFormula {
		display: flex;
		align-items: center;
		gap: 8px;
		flex-wrap: wrap;
		margin-top: 16px;
		padding: 12px 14px;
		border-radius: 16px;
		background: var(--CertificateAccentSoft);
		color: #66585b;
		font-size: 12px;
		font-weight: 900;
	}

	.CertificateFormula b {
		color: var(--CertificateAccentDark);
		font-size: 16px;
	}


	.CertificateDisclaimer {
		margin: 11px 2px 0;
		color: #918488;
		font-size: 11px;
		line-height: 1.7;
	}



	.CertificateRow.Muted {
		opacity: .7;
	}


	.CertificateRow.StampDue {
		background: color-mix(in srgb, var(--CertificateAccent) 28%, transparent);
	}



</style>





<style>



	@media (orientation: landscape) and (min-width: 761px) {
		.CertificateCostCalc {
			width: min(1120px, calc(100% - 24px));
			padding: clamp(18px, 3vw, 34px);
			border-radius: 30px;
		}

		.CertificateCostCalc.IsPlaced {
			width: var(--PlaceWidthL);
			height: var(--PlaceHeightL);
		}

		.CertificateHead {
			grid-template-columns: minmax(0, 1fr) 150px;
			gap: 24px;
		}

		.CertificateRateBadge {
			min-height: 128px;
			text-align: center;
		}

		.CertificateRateBadge strong {
			font-size: 38px;
		}

		.CertificateBody {
			grid-template-columns: minmax(300px, .95fr) minmax(330px, 1.05fr);
		}

		.CertificateInputs {
			padding: 18px;
			border-radius: 24px;
		}

		.CertificateResult {
			padding: 18px;
			border-radius: 24px;
		}

		.CertificateFormula {
			justify-content: center;
		}
	}


	@media (orientation: portrait), (max-width: 760px) {
		.CertificateCostCalc {
			width: min(100% - 14px, 600px);
			padding: 15px;
			border-radius: 22px;
		}

		.CertificateCostCalc.IsPlaced {
			width: var(--PlaceWidthP);
			height: var(--PlaceHeightP);
		}

		.CertificateHead {
			grid-template-columns: 1fr;
			gap: 15px;
		}

		.CertificateRateBadge {
			min-height: 82px;
			grid-template-columns: auto minmax(0, 1fr);
			justify-content: start;
			text-align: right;
		}

		.CertificateRateBadge strong {
			font-size: 30px;
		}

		.CertificateBody {
			grid-template-columns: 1fr;
		}

		.CertificateBuildingExtraOptions {
			grid-template-columns: 1fr;
		}

		.CertificateInputs {
			padding: 14px;
			border-radius: 18px;
		}

		.CertificateResult {
			padding: 14px;
			border-radius: 18px;
		}

		.CertificateFormula {
			justify-content: flex-start;
		}
	}



</style>
