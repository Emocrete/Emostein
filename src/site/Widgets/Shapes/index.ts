import BadgeFrameIndexExport from "./Frames/BadgeFrame.astro";

/**
 * @pBadge as string (Default: "NEW")
 * @pBg as string (Default: "#FAFAFA")
 * @pBadgeBg as string (Default: "#A7C45C")
 * @pColor as string (Default: "#111111")
 * @pHzAlign as HzAlignValue (Default: "Stretch")
 * @pVrAlign as VrAlignValue (Default: "Center")
 */
export const BadgeFrame = BadgeFrameIndexExport;

import BlobDividerIndexExport from "./Edges/BlobDivider.astro";

/**
 * @pHeight as NumPer (Default: new NumPer(150, 110))
 * @pFill as string (Default: "#A7C45C")
 * @pFill2 as string (Default: "rgba(255,255,255,.38)")
 * @pStroke as string (Default: "rgba(255,255,255,.55)")
 * @pStrokeWidth as number (Default: 0)
 */
export const BlobDivider = BlobDividerIndexExport;

import BlurBlobIndexExport from "./Decor/BlurBlob.astro";

/**
 * @pColor as string (Default: "rgba(50,143,232,.45)")
 * @pColor2 as string (Default: "rgba(167,196,92,.4)")
 * @pBlur as number (Default: 18)
 */
export const BlurBlob = BlurBlobIndexExport;

import CircleGlowIndexExport from "./Decor/CircleGlow.astro";

/**
 * @pColor as string (Default: "rgba(167,196,92,.75)")
 * @pColor2 as string (Default: "rgba(251,255,0,.26)")
 * @pBlur as number (Default: 28)
 */
export const CircleGlow = CircleGlowIndexExport;

import DiagonalBottomIndexExport from "./Edges/DiagonalBottom.astro";

/**
 * @pHeight as NumPer (Default: new NumPer(150, 110))
 * @pFill as string (Default: "#A7C45C")
 * @pFill2 as string (Default: "rgba(255,255,255,.38)")
 * @pStroke as string (Default: "rgba(255,255,255,.55)")
 * @pStrokeWidth as number (Default: 0)
 */
export const DiagonalBottom = DiagonalBottomIndexExport;

import DiagonalTopIndexExport from "./Edges/DiagonalTop.astro";

/**
 * @pHeight as NumPer (Default: new NumPer(150, 110))
 * @pFill as string (Default: "#A7C45C")
 * @pFill2 as string (Default: "rgba(255,255,255,.38)")
 * @pStroke as string (Default: "rgba(255,255,255,.55)")
 * @pStrokeWidth as number (Default: 0)
 */
export const DiagonalTop = DiagonalTopIndexExport;

import DoubleWaveIndexExport from "./Edges/DoubleWave.astro";

/**
 * @pHeight as NumPer (Default: new NumPer(150, 110))
 * @pFill as string (Default: "#A7C45C")
 * @pFill2 as string (Default: "rgba(255,255,255,.38)")
 * @pStroke as string (Default: "rgba(255,255,255,.55)")
 * @pStrokeWidth as number (Default: 0)
 */
export const DoubleWave = DoubleWaveIndexExport;

import FloatingIconIndexExport from "./Decor/FloatingIcon.astro";

/**
 * @pIcon as "star" | "check" | "spark" | "pin" (Default: "spark")
 * @pFill as string (Default: "#111111")
 * @pBg as string (Default: "#fbff00")
 * @pSpeed as number (Default: 3200)
 */
export const FloatingIcon = FloatingIconIndexExport;

import FloatingPanelIndexExport from "./Frames/FloatingPanel.astro";

/**
 * @pBg as string (Default: "rgba(17,24,32,.88)")
 * @pBorder as string (Default: "rgba(255,255,255,.18)")
 * @pAccent as string (Default: "#fbff00")
 * @pHzAlign as HzAlignValue (Default: "Stretch")
 * @pVrAlign as VrAlignValue (Default: "Center")
 */
export const FloatingPanel = FloatingPanelIndexExport;

import GlassCardIndexExport from "./Frames/GlassCard.astro";

/**
 * @pBg as string (Default: "rgba(255,255,255,.72)")
 * @pBorder as string (Default: "rgba(255,255,255,.72)")
 * @pRadius as number (Default: 28)
 * @pBlur as number (Default: 16)
 * @pHzAlign as HzAlignValue (Default: "Stretch")
 * @pVrAlign as VrAlignValue (Default: "Center")
 */
export const GlassCard = GlassCardIndexExport;

import ImageCircleIndexExport from "./Frames/ImageCircle.astro";

/**
 * @pSrc as string (Default: "/Media/TrustedIcon.webp")
 * @pAlt as string (Default: "")
 * @pBorder as string (Default: "#A7C45C")
 * @pCaption as string (Default: "")
 */
export const ImageCircle = ImageCircleIndexExport;

import LinePathIndexExport from "./Decor/LinePath.astro";

/**
 * @pStroke as string (Default: "rgba(255,255,255,.72)")
 * @pDot as string (Default: "#FAFAFA")
 * @pWidth as number (Default: 3)
 * @pAnimate as boolean (Default: true)
 */
export const LinePath = LinePathIndexExport;

import MountainCurveIndexExport from "./Edges/MountainCurve.astro";

/**
 * @pHeight as NumPer (Default: new NumPer(150, 110))
 * @pFill as string (Default: "#A7C45C")
 * @pFill2 as string (Default: "rgba(255,255,255,.38)")
 * @pStroke as string (Default: "rgba(255,255,255,.55)")
 * @pStrokeWidth as number (Default: 0)
 */
export const MountainCurve = MountainCurveIndexExport;

import RibbonIndexExport from "./Decor/Ribbon.astro";

/**
 * @pText as string (Default: "Emostein")
 * @pBg as string (Default: "#A7C45C")
 * @pColor as string (Default: "#111111")
 */
export const Ribbon = RibbonIndexExport;

import SoftArcIndexExport from "./Edges/SoftArc.astro";

/**
 * @pHeight as NumPer (Default: new NumPer(150, 110))
 * @pFill as string (Default: "#A7C45C")
 * @pFill2 as string (Default: "rgba(255,255,255,.38)")
 * @pStroke as string (Default: "rgba(255,255,255,.55)")
 * @pStrokeWidth as number (Default: 0)
 */
export const SoftArc = SoftArcIndexExport;

import SoftShadowIndexExport from "./Decor/SoftShadow.astro";

/**
 * @pColor as string (Default: "rgba(0,0,0,.26)")
 */
export const SoftShadow = SoftShadowIndexExport;

import SplashIndexExport from "./Decor/Splash.astro";

/**
 * @pFill as string (Default: "#A7C45C")
 * @pFill2 as string (Default: "rgba(251,255,0,.52)")
 */
export const Splash = SplashIndexExport;

import SvgDotsIndexExport from "./Decor/SvgDots.astro";

/**
 * @pColor as string (Default: "rgba(255,255,255,.35)")
 * @pGap as number (Default: 26)
 * @pDot as number (Default: 2)
 */
export const SvgDots = SvgDotsIndexExport;

import TiltCardIndexExport from "./Frames/TiltCard.astro";

/**
 * @pBg as string (Default: "#353535")
 * @pColor as string (Default: "#FAFAFA")
 * @pTilt as number (Default: -3)
 * @pHzAlign as HzAlignValue (Default: "Stretch")
 * @pVrAlign as VrAlignValue (Default: "Center")
 */
export const TiltCard = TiltCardIndexExport;

import TornPaperIndexExport from "./Edges/TornPaper.astro";

/**
 * @pHeight as NumPer (Default: new NumPer(150, 110))
 * @pFill as string (Default: "#A7C45C")
 * @pFill2 as string (Default: "rgba(255,255,255,.38)")
 * @pStroke as string (Default: "rgba(255,255,255,.55)")
 * @pStrokeWidth as number (Default: 0)
 */
export const TornPaper = TornPaperIndexExport;

import WaveBottomIndexExport from "./Edges/WaveBottom.astro";

/**
 * @pHeight as NumPer (Default: new NumPer(150, 110))
 * @pFill as string (Default: "#A7C45C")
 * @pFill2 as string (Default: "rgba(255,255,255,.38)")
 * @pStroke as string (Default: "rgba(255,255,255,.55)")
 * @pStrokeWidth as number (Default: 0)
 */
export const WaveBottom = WaveBottomIndexExport;

import WaveTopIndexExport from "./Edges/WaveTop.astro";

/**
 * @pHeight as NumPer (Default: new NumPer(150, 110))
 * @pFill as string (Default: "#A7C45C")
 * @pFill2 as string (Default: "rgba(255,255,255,.38)")
 * @pStroke as string (Default: "rgba(255,255,255,.55)")
 * @pStrokeWidth as number (Default: 0)
 */
export const WaveTop = WaveTopIndexExport;
