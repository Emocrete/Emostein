import BottomTriangleSepIndexExport from "./BottomTriangleSep.astro";

/**
 * @pTopColor as string (Default: "#d9e8f2")
 * @pBottomColor as string (Default: "#9c3636")
 * @pHeight as number (Default: 500)
 * @pTipX as number (Default: 100)
 * @pTipY as number (Default: 400)
 * @pContentWidth as number (Default: 1000)
 * @pPadInline as number (Default: 25)
 */
export const BottomTriangleSep = BottomTriangleSepIndexExport;

import BottTriSepIndexExport from "./BottomTriangleSep.astro";

/**
 * @pTopColor as string (Default: "#d9e8f2")
 * @pBottomColor as string (Default: "#9c3636")
 * @pHeight as number (Default: 500)
 * @pTipX as number (Default: 100)
 * @pTipY as number (Default: 400)
 * @pContentWidth as number (Default: 1000)
 * @pPadInline as number (Default: 25)
 */
export const BottTriSep = BottTriSepIndexExport;

import IncLineSepIndexExport from "./IncLineSep.astro";

/**
 * @pTopColor as string (Default: "#d9e8f2")
 * @pBottomColor as string (Default: "#9c3636")
 * @pHeight as number (Default: 500)
 * @pBottomLeft as number (Default: 100)
 * @pBottomRight as number (Default: 400)
 * @pContentWidth as string | number (Default: 1000)
 * @pPadInline as number (Default: 25)
 */
export const IncLineSep = IncLineSepIndexExport;

import WavesIndexExport from "./Waves.astro";

/**
 * @fHeight as number (Default: 80)
 * @fColor1 as string (Default: "#2d2f32")
 * @fColor2 as string (Default: "rgba(45,47,50,.55)")
 * @fFlip as boolean (Default: false)
 * @fSpeed1 as number (Default: 10)
 * @fSpeed2 as number (Default: 14)
 */
export const Waves = WavesIndexExport;
