---
import { ElemBg, GetHzAlignCss, GetVrAlignCss, type HzAlignValue, Percent, Pixel, Place, type PlaceProps, type VrAlignValue } from "@/TypesLib";
import type { HTMLAttributes } from "astro/types";

interface Props extends DebugOutlineProps, HTMLAttributes<"div">, PlaceProps {
	Centered?: boolean;
	centered?: boolean;
	pHzAlign?: HzAlignValue;
	pVrAlign?: VrAlignValue;
	pGap?: Pixel;
	pPad?: Pixel;
	pBg?: ElemBg;
	pBgColor?: ElemBg;
	pBgOpacity?: Percent;
	pBlur?: Pixel;
}

const cProps = Astro.props as Props & Record<string, unknown>;

const {
	Centered = false,
	centered = false,
	pHzAlign,
	pVrAlign,
	pGap = new Pixel(18, 12),
	pPad = new Pixel(40, 20),
	pBg,
	pBgColor,
	pBgOpacity = new Percent(100),
	pBlur = new Pixel(0),

	pLoc,
	pSize,
	pRotate,
	pZ,
	pOrigin,
	pOpacity,
	pClickable,
	pLandOnly,
	pPortOnly,
	pMove,
	pMoveDuration,
	pMoveLoc,
	pMoveBox,
	pMoveDistance,
	pMoveKeyPoints,
	pMoveIntervals,
	pMoveDelay,
	pMoveEase,
	pMoveLoop,
	pMoveLoopFor,
	pMoveLoopInterval,
	pMoveReverse,
	pMoveStartOnView,
	pMoveReplayOnView,
	pMovePauseWhenOut,
	pMoveViewThreshold,
	pMoveViewMargin,
	pMoveViewTarget,

	id: pGivenID = GetWidgetId("DivCol"),
	class: pGivenClass = "",
	style: pGivenStyle = "",
	...RemAttr
} = cProps;

const cIsCentered = Centered || centered || GetBoolAttr(cProps["data-centered"]);
const cBg = pBg ?? pBgColor;
const cHzAlign = cIsCentered ? "Center" : pHzAlign ?? "Center";
const cVrAlign = cIsCentered ? "Center" : pVrAlign ?? "Start";

const cHasWidthL = HasExplicitCssSize(pSize?.WidthL);
const cHasWidthP = HasExplicitCssSize(pSize?.WidthP);
const cHasHeightL = HasExplicitCssSize(pSize?.HeightL);
const cHasHeightP = HasExplicitCssSize(pSize?.HeightP);

const cStyle = [
	cHasWidthL ? `--DivColWidthL:${pSize?.WidthL};` : "",
	cHasWidthP ? `--DivColWidthP:${pSize?.WidthP};` : "",
	cHasHeightL ? `--DivColHeightL:${pSize?.HeightL};` : "",
	cHasHeightP ? `--DivColHeightP:${pSize?.HeightP};` : "",
	`--DivColGapL:${pGap.Landscape};`,
	`--DivColGapP:${pGap.Portrait};`,
	`--DivColPadL:${pPad.Landscape};`,
	`--DivColPadP:${pPad.Portrait};`,
	`--DivColBg:${cBg?.Color || "transparent"};`,
	`--DivColBgOpacityL:${pBgOpacity.Landscape};`,
	`--DivColBgOpacityP:${pBgOpacity.Portrait};`,
	`--DivColBlurL:${pBlur.Landscape};`,
	`--DivColBlurP:${pBlur.Portrait};`,
	`--DivColAlignItems:${GetHzAlignCss(cHzAlign)};`,
	`--DivColJustifyContent:${GetVrAlignCss(cVrAlign)};`,
	GetStyleText(pGivenStyle),
].filter(Boolean).join(" ");

const cPlace = new Place({
	pLoc,
	pSize,
	pRotate,
	pZ,
	pOrigin,
	pOpacity,
	pClickable,
	pLandOnly,
	pPortOnly,
	pMove,
	pMoveDuration,
	pMoveLoc,
	pMoveBox,
	pMoveDistance,
	pMoveKeyPoints,
	pMoveIntervals,
	pMoveDelay,
	pMoveEase,
	pMoveLoop,
	pMoveLoopFor,
	pMoveLoopInterval,
	pMoveReverse,
	pMoveStartOnView,
	pMoveReplayOnView,
	pMovePauseWhenOut,
	pMoveViewThreshold,
	pMoveViewMargin,
	pMoveViewTarget,
} as PlaceProps, cStyle);

function GetBoolAttr(pValue: unknown) {
	return pValue === true || pValue === "" || pValue === "true";
}

function HasExplicitCssSize(pValue?: string) {
	const cValue = `${pValue ?? ""}`.trim().toLowerCase();
	return cValue !== "" && cValue !== "auto" && cValue !== "fit-content" && cValue !== "max-content" && cValue !== "min-content";
}

function GetStyleText(pStyle: unknown) {
	if (!pStyle) return "";
	if (typeof pStyle === "string") return pStyle;
	if (typeof pStyle !== "object") return "";

	return Object.entries(pStyle as Record<string, string | number | null | undefined>)
		.map(([pName, pValue]) => {
			if (pValue === undefined || pValue === null) return "";
			return `${GetCssName(pName)}:${pValue};`;
		})
		.filter(Boolean)
		.join(" ");
}

function GetCssName(pName: string) {
	if (pName.startsWith("--")) return pName;
	return pName.replace(/[A-Z]/g, (pChar) => `-${pChar.toLowerCase()}`);
}
---




















<div
	class = {`DivCol ${cIsCentered ? "IsCentered" : "IsTop"} ${cPlace.Class} ${pGivenClass}`.trim()}
	style = {cPlace.Style}
	id={pGivenID}
	data-duo-width-l={cHasWidthL ? "true" : undefined}
	data-duo-width-p={cHasWidthP ? "true" : undefined}
	data-emo-auto-container="true"
	data-emo-explicit-height={cHasHeightL || cHasHeightP ? "true" : "false"}
	data-emo-empty-height="200"
	{...RemAttr}
>







	<slot />







</div>




















<style>



	.DivCol {
		position: relative;
		display: flex;
		flex-direction: column;
		box-sizing: border-box;
		width: var(--DivColWidthL, 100%);
		height: var(--DivColHeightL, auto);
		max-width: 100%;
		min-width: 0;
		padding: var(--DivColPadL, 0px);
		gap: var(--DivColGapL, 0px);
		align-items: var(--DivColAlignItems, center);
		justify-content: var(--DivColJustifyContent, flex-start);
		background-color: color-mix(in srgb, var(--DivColBg, transparent) var(--DivColBgOpacityL, 100%), transparent);
		backdrop-filter: blur(var(--DivColBlurL, 0px));
		-webkit-backdrop-filter: blur(var(--DivColBlurL, 0px));
		overflow: visible;
	}

	.DivCol > :global(*) {
		box-sizing: border-box;
		max-width: 100%;
		min-width: 0;
	}

	.DivCol > :global(.Emograph:not(.IsPlaced):not(.PlaceSized)) {
		width: 100%;
	}

	.DivCol > :global(.EmoGallery:not(.IsPlaced):not(.PlaceSized)) {
		width: 100%;
	}



	.DivCol.IsPlaced {
		position: absolute;
		width: var(--PlaceWidthL);
		height: var(--PlaceHeightL);
	}



</style>




















<style>



	@media (orientation: portrait) {
		.DivCol {
			width: var(--DivColWidthP, var(--DivColWidthL, 100%));
			height: var(--DivColHeightP, var(--DivColHeightL, auto));
			padding: var(--DivColPadP, var(--DivColPadL, 0px));
			gap: var(--DivColGapP, var(--DivColGapL, 0px));
			background-color: color-mix(in srgb, var(--DivColBg, transparent) var(--DivColBgOpacityP, var(--DivColBgOpacityL, 100%)), transparent);
			backdrop-filter: blur(var(--DivColBlurP, var(--DivColBlurL, 0px)));
			-webkit-backdrop-filter: blur(var(--DivColBlurP, var(--DivColBlurL, 0px)));
		}

		.DivCol.IsPlaced {
			width: var(--PlaceWidthP);
			height: var(--PlaceHeightP);
		}
	}



</style>
