import SiteIntroIndexExport from "./SiteIntro.astro";

/**
 * @pLogo as string (Default: "/Media/EmoLogo.svg")
 * @pAlt as string (Default: "شعار الموقع")
 * @pBaseClr as string (Default: Colors.WhiteOff)
 * @pSweepClr as string (Default: Colors.Bistage)
 * @pLogoBgClr as string (Default: "transparent")
 * @pTotalMs as number (Default: 2600)
 * @pLogoWidth as string | RespString (Default: new RespString("600px", "250px"))
 * @pZ as number (Default: 999999)
 * @pOnceKey as string (Default: "")
 * @pEase as string (Default: "cubic-bezier(.42, 0, .18, 1)")
 */
export const SiteIntro = SiteIntroIndexExport;
